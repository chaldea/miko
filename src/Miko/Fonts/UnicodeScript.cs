namespace Miko.Fonts;

/// <summary>
/// Unicode script categories for font fallback resolution
/// </summary>
public enum UnicodeScript
{
    Latin,
    CJK,        // Chinese, Japanese, Korean
    Cyrillic,
    Arabic,
    Hebrew,
    Thai,
    Symbol,
    Unknown
}
